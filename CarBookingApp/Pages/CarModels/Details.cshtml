@page
@model CarBookingApp.Pages.Makes.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Make</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Make.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Make.Name)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-warning" asp-page="./Update" asp-route-id="@Model.Make.Id">Edit</a> |
    <a class="btn btn-primary" asp-page="./Index">Back to List</a>
</div>
